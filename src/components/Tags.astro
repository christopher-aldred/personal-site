---
const { tags } = Astro.props;
---

{
	tags.map((tag) => (
		<a
			href={`/tags/${tag}`}
			class="mr-2 inline-block rounded-full bg-neutral-500/10 px-3 py-1 text-xs text-neutral-700 dark:bg-neutral-800 dark:bg-white/10 dark:text-neutral-300"
		>
			{tag}
		</a>
	))
}
